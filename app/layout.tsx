import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Limitless IT Solutions | Professional IT Support & Services",
  description:
    "Fast, remote, and affordable IT solutions for businesses and individuals. Expert support for networking, cybersecurity, cloud services, and more.",
  generator: "v0.app",
  keywords: "IT support, network setup, cybersecurity, cloud services, remote troubleshooting, business IT solutions",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable} antialiased`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
